import React, {FC} from 'react';

const HeaderComponent: FC = () => {
    return (
        <ul className="d-flex align-items-center w-100 bg-secondary-subtle list-unstyled"></ul>
    );
};

export default HeaderComponent;
